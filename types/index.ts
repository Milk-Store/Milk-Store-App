export interface Slider {
  id: string;
  image: string;
  title?: string;
  description?: string;
}

export interface BannerPopup {
  id: string;
  image: string;
  title: string;
  description: string;
  isActive: boolean;
} 